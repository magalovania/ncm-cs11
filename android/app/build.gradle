plugins {
  id 'com.android.application'
}

android {
  namespace 'com.ncmcs11'
  compileSdk 34

  defaultConfig {
    applicationId "com.ncmcs11"
    minSdk 19
    targetSdk 34
    versionCode 14
    versionName "1.2.3"
    buildConfigField "boolean", "SCREENSHOT_FEEDBACK_ENABLED", "false"
  }

  sourceSets {
    main {
      // Bundle the lightweight web client for API 19; API calls still use the configured server.
      assets.srcDir '../../web'
    }
  }

  buildTypes {
    debug {
      minifyEnabled false
    }
    feedback {
      initWith debug
      matchingFallbacks = ['debug']
      buildConfigField "boolean", "SCREENSHOT_FEEDBACK_ENABLED", "true"
    }
    release {
      minifyEnabled false
    }
  }

  buildFeatures {
    buildConfig true
  }

  compileOptions {
    sourceCompatibility JavaVersion.VERSION_1_8
    targetCompatibility JavaVersion.VERSION_1_8
  }
}

// No external dependencies — pure Android framework APIs (WebView + MediaSession + Notification).
dependencies {
}
