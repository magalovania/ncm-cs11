# ncm-cs11

领克 CS11 老款安卓车机的网易云音乐客户端。

- `server/`  VPS 端：NeteaseCloudMusicApi + Web UI 静态托管 + 部署脚本
- `web/`     移动优先音乐播放器 Web UI
- `android/` 车机 APK（WebView 壳 + MediaSession/前台服务）

详见 `CLAUDE.md`（本地）。